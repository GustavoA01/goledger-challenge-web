import { GoBackButton } from "@/src/components/GoBackButton"

const WatchListPage = () => {
  return <div className="container mx-auto p-4 max-w-2xl">
    <header className="flex items-center gap-4 mt-4">
      <GoBackButton/>
      <h1 className="text-xl semi-bold">Nova lista</h1>
    </header>

    <form>
      
    </form>
  </div>
}

export default WatchListPage
